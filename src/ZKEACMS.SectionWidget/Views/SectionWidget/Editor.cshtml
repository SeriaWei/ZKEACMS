@using ZKEACMS.SectionWidget.Models
@model ZKEACMS.SectionWidget.Models.SectionWidget
@{
    Layout = null;
}

@foreach (SectionGroup group in Model.Groups.OrderBy(m => m.Order))
{
    await Html.RenderPartialAsync("SectionGroup.Edit", group);
}
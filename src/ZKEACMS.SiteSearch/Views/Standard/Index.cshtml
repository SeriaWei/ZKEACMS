@using ZKEACMS.SiteSearch.Models
@model Standard
@{
    Style.Reqiured("dataTable").AtHead();
    Script.Reqiured("dataTable").AtFoot();

    Script.Reqiured("datepicker").AtFoot();
    Style.Reqiured("datepicker").AtHead();

}
@if (Authorizer.Authorize(ZKEACMS.SiteSearch.PermissionKeys.ManageStandard))
{
    <div id="toolBar">
        <a href="@Url.Action("Create")" class="btn btn-primary btn-sm">新建</a>
    </div>
}
<grid delete-able="Authorizer.Authorize(ZKEACMS.SiteSearch.PermissionKeys.ManageStandard)" edit-able="Authorizer.Authorize(ZKEACMS.SiteSearch.PermissionKeys.ManageStandard)"></grid>

@using Easy.Modules.Role
@{ 
    Style.Reqiured("dataTable").AtHead();
    Script.Reqiured("dataTable").AtFoot();
	
	Script.Reqiured("datepicker").AtFoot();
    Style.Reqiured("datepicker").AtHead();
}
@if (Authorizer.Authorize(PermissionKeys.ManageRole))
{
    <div id="toolBar">
        <a href="@Url.Action("Create")" class="btn btn-primary btn-sm">@L("新建")</a>
    </div>
}

<grid model-type="@(typeof(RoleEntity))" order-desc="LastUpdateDate" delete-able="Authorizer.Authorize(PermissionKeys.ManageRole)" edit-able="Authorizer.Authorize(PermissionKeys.ManageRole)"></grid>
@{
    ViewBag.Title = ZKEACMS.Version.CurrentVersion + " " + ZKEACMS.Version.Rank + " 深圳市纸壳软件科技有限公司";
    Layout = Layouts.NormalDefault;
    Style.Reqiured("Customer").AtHead();
    Script.Reqiured("validate").AtFoot();
}

@using (Html.BeginForm())
{

    <div class="login-panel forgotten">
        <div class="login-panel-heading">
            <p>
                <a href="http://www.zkea.net/" target="_blank">
                    <img class="logo" src="~/images/logo_min.png" alt="ZKEACMS" />
                </a>
            </p>
        </div>
        <div class="login-panel-body">
            <div class="info-bar">
                <p>
                    请输入您的用户信息以找回密码
                </p>
                <img src="~/css/Images/picker_account_msa.svg" />
            </div>
            <div class="form-input">
                <h4>邮箱</h4>
                <input type="text" 
                       placeholder="输入您注册时使用的邮箱地址" 
                       id="Email" 
                       name="Email" 
                       class="form-control"
                       data-val="true" 
                       data-val-required="找回密码的邮箱不能为空"
                       data-val-length="邮箱的长度应小于200"
                       data-val-regex-pattern="@(Easy.Constant.RegularExpression.Email)"
                       data-val-regex="输入的邮件格式不正确"
                       data-val-length-max="200"/>
                <span class="field-validation-valid " data-valmsg-for="Email" data-valmsg-replace="true"></span>
            </div>
            @if (ViewBag.Errormessage != null)
            {
                <div class="text-center alert alert-danger">
                    @ViewBag.Errormessage
                </div>
            }
            <div class="form-input">
                <input type="submit" class="btn-login" value="找回密码" />
            </div>
        </div>
        <div class="login-panel-footer ">
           想起密码了？ <a href="@Url.Action("SignIn")">立即登录</a>
        </div>
    </div>
}
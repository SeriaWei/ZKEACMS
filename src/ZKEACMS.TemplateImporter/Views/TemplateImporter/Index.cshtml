@using ZKEACMS.Standard.Models
@model Standard
@{
    Style.Reqiured("dataTable").AtHead();
    Script.Reqiured("dataTable").AtFoot();

    Script.Reqiured("datepicker").AtFoot();
    Style.Reqiured("datepicker").AtHead();

}
@if (Authorizer.Authorize(ZKEACMS.Standard.PermissionKeys.ManageStandard))
{
    <div id="toolBar">
        <a href="@Url.Action("Create")" class="btn btn-primary btn-sm">新建</a>
    </div>
}
<grid delete-able="Authorizer.Authorize(ZKEACMS.Standard.PermissionKeys.ManageStandard)" edit-able="Authorizer.Authorize(ZKEACMS.Standard.PermissionKeys.ManageStandard)"></grid>
